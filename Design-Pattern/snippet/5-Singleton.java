
class Singleton {
	private Singleton singleton_object;

	private class Singleton()

	public Singleton getObject {
		if (singleton_object == null) singleton_object = new Singleton();
		return singleton_object
	}
}